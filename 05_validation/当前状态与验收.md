# 当前状态与验收（权威入口）

本文件是当前状态的权威入口；`多Agent复审报告.md`保留为历史审计记录，不覆盖本文件。

## 状态

| 项目 | 状态 | 说明 |
|---|---|---|
| 模型语义 | FROZEN | 变量、硬约束、时间索引、能源口径已冻结 |
| 2406 时间门禁 | PASSED | 能源动作 `0..2406`，任务不得占用 2406 |
| `B3_ref`/`x0` 负荷门禁 | PASSED | 最大 AI IT 负荷误差 `3.3333448e-7 MW` |
| `x0` 可行性 | FAILED AS BASELINE | 44 个 GPU 超限小时；仅用于附件负荷复现 |
| `x_base` 任务门禁 | PASSED | 50,000 个任务全部排程，90 个弹性任务延后，算力层零违约 |
| Q1 预测 | PASSED | 主模型：TaskCount/GPU_Demand/GPU_Hour→LagRF，AvgDuration→HW24；测试指标见 `04_algorithms/results/q1/scores.csv` |
| Q1B 基础调度 | PASSED | `x_Q1` 与 `x_base` 同规则同排程（50,000 任务零违约，5,020,787 GPU-hour） |
| 能源四格 M00/M01/Q3 | PASSED | 全部能源断言零违约（守恒/SOC/互斥/边界）；M00 无储能无外送，M01 储能+外送 |
| Q2/M10 数值求解 | DONE | 加权标量化近似非支配解（贪心+局部搜索），诚实标记非全局最优；小样本 gap 见 `04_algorithms/results/q2_gap/` |
| Q3 题设实验 | DONE | `B3_ref` 固定负荷+储能，独立报告，不与四格 M01 混称 |
| Q4/M11 数值求解 | DONE | 任务变量独立重优化（不锁死 Q2）+ 含储能能源层 |
| `S_K` | CALCULATED | 推荐场景 λ=100；定义与数值见 `04_algorithms/results/summary/` |
| 论文数值结论 | 可引用 | 数值已回传（`04_algorithms/results/kpi_summary.csv`），论文须保持诚实标记与口径说明 |

## 已通过的门禁

- `SOC_-1=InitialSOC_MWh`，`SOC_t`为小时末状态，终端要求 `SOC_2406>=InitialSOC_MWh`。
- `B3_ref` 与来源区域、到达即启动的 `x0` 的 AI IT 负荷最大误差 `3.3333448e-7 MW`；14,442 个区域小时均在 `1e-6 MW` 内。
- `x0` 容量快检发现 44 个 GPU 超限，最大为 RegionF Hour 229 的 `1309.733/966=135.58%`，已禁止其作为四格基准。
- `x_base` 输出为 `02_data/processed/x_base_task_schedule.csv`：50,000 条任务，90 条弹性任务延后，唯一指派、实时即开、截止期、本地 SLA、GPU、IT、设施功率零违约。最大 GPU 利用率 `99.9931%`。

## 算法手已交付（2026-08-26）

1. Q1 预测：主模型选择完成（TaskCount/GPU_Demand/GPU_Hour → 滞后特征 RF；AvgDuration → Holt-Winters s=24），
   验证/测试指标与逐系列预测见 `04_algorithms/results/q1/`；最后 24 小时甘特数据 `gantt_last24h.csv`。
2. Q1B 基础调度：`x_Q1` 输出（与 `x_base` 同规则同排程）`04_algorithms/results/q1/x_Q1_task_schedule.csv`。
3. Q2/M10：加权标量化近似非支配解（贪心+局部搜索，6 个碳价权重），任务/能源双层零违约；
   小样本（48h 窗口）CP-SAT 精确 gap 报告见 `04_algorithms/results/q2_gap/gap_report.md`。
4. Q3：`B3_ref` 题设固定负荷+储能实验（`output/Q3_B3ref/`），与四格 `M01` 分开命名。
5. Q4/M11：任务变量独立重优化 + 含储能能源层（不锁死 Q2 解）。
6. M00/M01/M10/M11 统一 KPI 表（`output/kpi_summary.csv`）、能源验收（`output/validation_report.md`）
   与 `S_K`（`output/summary/`）。

数据特性提示（影响论文叙事）：附件各区新能源可用量（均值约 800 MW）普遍大于设施负荷（约 450 MW），
仅 RegionF 存在 1 个 RE 缺口小时（31.6 MW）。因此四格成本/碳排绝对值都接近零，主要改善来自
新能源利用率（外送口径）与负荷时空分布；论文应据此组织“先 RE 直供消纳、再柔性”的叙事，
不宜把成本改善率作为唯一卖点。

## 结果诚信

近似算法不得声称全局 Pareto 最优（M10/M11 为加权标量化启发式近似解，已诚实标记）；
硬约束不可用目标函数惩罚替代；RegionE Hour 0 的约 `0.999944 MWh` SOC 源表残差已在
`solver_log.jsonl` 单列，未静默修复；`Cost_CNY` 为购电毛成本口径（DEC-008），售电收入与净成本
见扩展列。
